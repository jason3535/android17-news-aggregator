# 部署指南 - Android 17 & iOS 27 竞品追踪系统

## ✅ Git 提交完成

**最新提交**: `feat: 添加分享功能和AI总结 (Nothing 竞品追踪)`

**推送状态**: ✓ 已推送到 GitHub
**仓库地址**: https://github.com/jason3535/android17-news-aggregator

## 🚀 部署到 Railway

### 方法 1: 通过 Railway Dashboard（推荐）

1. **登录 Railway**
   - 访问: https://railway.app
   - 使用 GitHub 账号登录

2. **创建新项目或更新现有项目**

   **新项目:**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择 `jason3535/android17-news-aggregator`
   - Railway 会自动检测配置并部署

   **已有项目:**
   - 进入项目 Dashboard
   - 点击项目名称
   - Railway 会自动检测到新的 commit
   - 点击 "Deploy" 或等待自动部署

3. **等待部署完成**
   - 构建时间: ~2-3分钟
   - 部署成功后会显示 URL

4. **配置环境变量（如需要）**
   - 在 Variables 标签页添加环境变量
   - 目前不需要额外配置

### 方法 2: 通过 Railway CLI

```bash
# 安装 Railway CLI（如果还没有）
npm install -g @railway/cli

# 登录
railway login

# 链接项目（首次）
railway link

# 部署
railway up
```

## 🔧 部署配置说明

### Procfile
```
web: gunicorn app:app --workers=2 --threads=4 --worker-class=gthread
```

- **Workers**: 2个工作进程
- **Threads**: 每个进程4个线程
- **绑定端口**: Railway 自动分配的 $PORT

### Railway.json 配置
- ✅ 健康检查: `/api/status`
- ✅ 自动重启: 失败时重启（最多3次）
- ✅ 超时设置: 60秒

### Requirements.txt
所有依赖已配置:
- Flask 3.0.0
- gunicorn 21.2.0
- requests 2.31.0
- beautifulsoup4 4.12.2
- 其他必要依赖

## 📦 新功能部署清单

✅ **分享功能**
- `/api/share` - 创建分享链接
- `/share/<id>` - 分享页面
- 分享数据持久化

✅ **AI总结**
- `summarizer.py` - 智能总结模块
- 自动提取核心要点
- Nothing 风格展示

✅ **UI优化**
- 分享按钮优化
- Nothing 品牌风格统一
- 移动端适配

## 🌐 部署后访问

部署成功后，您的应用将在：
```
https://your-project-name.up.railway.app
```

### 测试功能

1. **主页**
   ```
   https://your-app.up.railway.app/
   ```

2. **分享功能**
   - 点击新闻卡片的 "⤴ 分享" 按钮
   - 生成分享链接
   - 访问分享页面查看 AI 总结

3. **API 状态**
   ```
   https://your-app.up.railway.app/api/status
   ```

## 📊 部署后监控

### Railway Dashboard

- **Logs**: 查看应用日志
- **Metrics**: CPU、内存、网络使用情况
- **Deployments**: 部署历史记录

### 健康检查

应用包含健康检查端点：
```bash
curl https://your-app.up.railway.app/api/status
```

返回示例：
```json
{
  "status": "running",
  "last_updated": "2026-02-15T08:30:00",
  "total_items": 45,
  "scheduler_running": true
}
```

## 🔄 自动部署

Railway 已配置自动部署：
- ✅ 每次 push 到 main 分支自动部署
- ✅ 自动运行健康检查
- ✅ 失败自动回滚

## 🐛 故障排查

### 部署失败

1. **检查日志**
   - Railway Dashboard → Logs
   - 查看具体错误信息

2. **常见问题**
   - Python 版本: 确保使用 Python 3.9+
   - 依赖问题: 检查 requirements.txt
   - 端口配置: 确保使用 `$PORT` 环境变量

### 应用无法访问

1. **检查健康检查**
   ```bash
   railway logs
   ```

2. **查看部署状态**
   - Railway Dashboard → Deployments
   - 确认最新部署成功

### 分享功能问题

1. **检查数据目录**
   - `data/shares.json` 应该被正确创建
   - 确保有写入权限

2. **测试 API**
   ```bash
   curl -X POST https://your-app.up.railway.app/api/share \
     -H "Content-Type: application/json" \
     -d '{"news_item": {...}}'
   ```

## 🎯 其他部署平台

### Vercel（备选）

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

### Heroku（备选）

```bash
# 登录 Heroku
heroku login

# 创建应用
heroku create your-app-name

# 推送代码
git push heroku main
```

### Render（备选）

1. 访问 https://render.com
2. 连接 GitHub 仓库
3. 选择 Web Service
4. 配置构建命令和启动命令
5. 部署

## 📝 环境变量配置（可选）

如果需要配置环境变量：

```bash
# Railway
railway variables set KEY=VALUE

# Heroku
heroku config:set KEY=VALUE

# Vercel
vercel env add KEY
```

目前应用不需要额外的环境变量即可运行。

## ✨ 功能验证清单

部署后请验证：

- [ ] 主页正常加载
- [ ] 新闻列表显示正常
- [ ] 分享按钮可见且样式正确
- [ ] 点击分享按钮生成链接
- [ ] 分享链接可访问
- [ ] AI 总结正常显示
- [ ] Nothing 风格统一
- [ ] 移动端适配正常

## 🎉 部署完成

恭喜！您的 Nothing 竞品追踪系统已成功部署！

**功能亮点：**
- ✅ 实时追踪 Android 17 & iOS 27 新闻
- ✅ 一键分享功能
- ✅ AI 智能总结
- ✅ Nothing 品牌风格设计

---

**更新时间**: 2026-02-15
**版本**: v2.0 - Share & AI Summary
